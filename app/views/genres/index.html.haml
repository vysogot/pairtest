.row
  .col-md-8.col-md-offset-2.col-lg-8.col-lg-offset-2
    %h1.text-center All genres
    %table.table.table-striped
      %thead
        %tr
          %th #
          %th Name
          %th Number of movies
          %th Created at
      %tbody
        - @genres.each_with_index do |genre, i|
          %tr
            %td= i + 1
            %td= link_to genre.name, movies_genre_path(genre.id)
            %td= genre.number_of_movies
            %td= genre.created_at
